<!DOCTYPE html>
<html>
<head>
  <style>
    .container { padding: 20px; font-family: Arial, sans-serif; }
    .header { color: #f59e0b; font-size: 24px; margin-bottom: 20px; }
    .warning { background: #fef3c7; padding: 15px; border-radius: 8px; border: 1px solid #f59e0b; }
    .document-list { margin: 20px 0; }
    .document { margin-bottom: 10px; padding: 10px; background: #f3f4f6; border-radius: 4px; }
    .button {
      background: #f59e0b;
      color: white;
      padding: 12px 24px;
      border-radius: 6px;
      text-decoration: none;
      display: inline-block;
      margin-top: 15px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      Documents à renouveler
    </div>
    <div class="warning">
      <p>Certains de vos documents arrivent à expiration. Pour continuer à vendre sur notre plateforme, veuillez les mettre à jour.</p>
    </div>
    
    <div class="document-list">
      {{#each documents}}
        <div class="document">
          <p>Document : <strong>{{type}}</strong></p>
          <p>Expire dans : <strong>{{daysRemaining}} jours</strong></p>
        </div>
      {{/each}}
    </div>

    <a href="{{updateUrl}}" class="button">
      Mettre à jour mes documents
    </a>
  </div>
</body>
</html> 